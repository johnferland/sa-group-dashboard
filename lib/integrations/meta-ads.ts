// TODO(Phase 1): port + adapt the Meta Marketing API fetcher from the previous build's
// integrations.ts (lead-action priority matcher). Uses a long-lived System User token
// generated fresh for this app's own Meta App ID — see META_SYSTEM_USER_TOKEN.

export async function syncMetaAdsForBrand(brandId: string, date: string) {
  throw new Error("not implemented yet");
}
