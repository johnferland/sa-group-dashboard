// TODO(Phase 1): port + adapt the Google Ads (GAQL) fetcher from the previous build's
// integrations.ts. Uses its own developer token + login customer ID, separate from the
// GA4/GSC OAuth client — see GOOGLE_ADS_DEVELOPER_TOKEN / GOOGLE_ADS_LOGIN_CUSTOMER_ID.

export async function syncGoogleAdsForBrand(brandId: string, date: string) {
  throw new Error("not implemented yet");
}
